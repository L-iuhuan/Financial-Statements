# 打包前检查清单（发布门禁）

> 每次发布前逐项执行。✅ = 2026-08-16 已实测通过；⬜ = 本次未执行。
> 相关：docs/RELEASE_AND_SIGNING.md（签名）、scripts/AGENTS.md（脚本约定）、人工核对清单-2026-08-16.md（C 区真实环境项）。

## 1. 代码门禁（本地，全绿才可构建）

- [x] ✅ `python -m ruff check src/ tests/` — All checks passed
- [x] ✅ `python -m mypy --no-incremental --ignore-missing-imports --python-version 3.12 src/fsa`（CI 口径）— 98 文件零错误
- [x] ✅ `python -m pytest -q` — 1537 passed / 0 failed（2026-08-16）
- [x] ✅ 覆盖率 86.29%（≥84 基线；core/services/storage ≥90 由 CI 门禁）
- [x] ✅ `python scripts/validate_real_data.py` — manifest 驱动，0 异常 + 0 未白名单失败，exit=0
- [x] ✅ `python scripts/validate_corpus.py` — 18 家语料全绿（白名单已建）

## 2. 版本与渠道一致性

- [x] ✅ `src/fsa/core/version.py` 的 APP_VERSION 与 installer.iss `MyAppVersion` 一致（0.4.1）；`python -m pytest tests/test_version_consistency.py -q` 通过
- [x] ✅ 双版本通道：通用版无域检查；内部版构建期固化 `_edition_override.py`，finally 自动删除防误提交；冻结版忽略 FSA_EDITION/FSA_DOMAIN_WHITELIST/FSA_UPDATE_MANIFEST_URL 环境变量（2026-08-16 修复，4 测试锁定）
- [x] ✅ 无 `_edition_override.py` 残留：`git status` 不含该文件

## 3. 构建与产物

- [x] ✅ `.ps1` 脚本全部 UTF-8 带 BOM（2026-08-16 修复——无 BOM 时 PS 5.1 按 GBK 解析中文注释导致语法错误，构建直接失败）
- [x] ✅ `powershell -ExecutionPolicy Bypass -File scripts\build_installer.ps1 -Edition general` 构建成功
- [x] ✅ 体积：**206 MB**（优化历程：333 → 244 剔环境泄漏包 → 224.6 剔未用 Qt DLL/翻译 → 206.4 剔 pydantic/lxml/html5lib 等深层泄漏+包内测试目录；原生底线 = Qt 核心 + opengl32sw 无 GPU 回退 + pandas/numpy；UPX 因杀软误报风险不用；Inno Setup LZMA 压缩后安装包预计 ~70-90MB）
- [x] ✅ 产物内容核查：`dist/fsa/_internal/resources/knowledge/`（9 个知识文档已入包）、`cas_gouji_rule_library.json`、logo 齐全；无 tests/、无真实报表、无 API key、无 pydantic/lxml/scipy
- [x] ✅ `scripts\smoke_package.ps1` — 通用版 exe 启动存活 + SQLite 建库通过（裁剪后实测）
- [x] ✅ **内部版闸门实测**（2026-08-16）：`-Edition internal -DomainWhitelist corp.example.com` 构建成功；本机（未入域）启动内部版 → 弹出「内部版域控制」拦截框（QMessageBox critical，含中文原因）拒绝进入主界面（app.py:154-170 验证）——授权闸门对未入域机器生效
- [x] ✅ **内部版入域机器放行实测**（2026-08-17）：生产域参数固化（DNS 名 `toll.cn` + NetBIOS 名 `TOLL` 兜底，详见 RELEASE_AND_SIGNING.md §2.0）；Inno Setup 安装器编译成功（`installer_output\财务报表勾稽校验系统_内部版_Setup_0.4.1.exe`，70.2MB）；入域机器（910373@toll.cn）启动 `dist\fsa\fsa.exe` 域检查通过正常进入主界面；设 `FSA_EDITION=general` 不绕过（frozen 忽略环境变量）
- [x] ✅ Inno Setup 安装器编译（2026-08-17 本机已装 Inno Setup 6，内部版安装器已产出）
- ⬜ Authenticode 签名（`sign_build.ps1`，需证书）

## 4. 功能抽测（打包前最后一遍，offscreen 或真机）

- [x] ✅ 多主体批量校验端到端（两主体 synthetic 文件夹：各 23 过/2 不通过/0 异常；双边核对正常执行）✅ 2026-08-16
- [x] ✅ 性能基准：导入+5 轮校验 0.07s；历史 200 条写入 0.03s / 加载搜索 0.12s；主题切换基准 65ms/次
- ⬜ DLP 加密环境真实文件导入（COM 回退 + 行号偏移修复验证）——需入域加密机
- ⬜ 真机人工过目 UI 一轮（offscreen 截屏不覆盖真实 DPI/系统圆角）

## 5. 仓库卫生

- [x] ✅ `git status` 无 custom_rules.json（已出 git + gitignore）、无 `_edition_override.py`、无 tests/fixtures/real_reports/ 内容
- [x] ✅ AGENTS.md 系列与代码一致（2026-08-16 校准）；HANDOFF/README 无过时承诺
- [ ] 提交前确认改动清单经负责人过目（当前 130+ 文件改动在工作区未 commit）
