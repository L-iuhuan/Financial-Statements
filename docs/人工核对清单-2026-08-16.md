# 人工核对与处理清单 — 2026-08-16

> 来源：端到端审查（审查报告-2026-08-16.md）+ 修复轮 + 18 家真实年报语料回归（东方财富 akshare 数据源，2025 年报）。
> 用途：列出**必须由人（财务/准则专家或开发负责人）核对或决策**的事项。每项给出证据、建议处置与预估工作量。
> 本清单不含已修复项（P0×1、P1×11 及约 30 项 P2/P3 已于 2026-08-16 修复并测试通过）。

---

## A. 真实年报语料回归结果（2026-08-16 首次建立）

**数据源**：东方财富 akshare，18 家 A 股公司 2025 年报三大报表（金融 4 / 地产 1 / 建筑 1 / 零售 1 / 一般 5 / 周期 4 / 高增长 3 —— 以 manifest.json 为准），存于 `tests/fixtures/real_reports/`（gitignored，严禁提交）。

**稳定性结论**：**18 家全部 errored=0**（无执行异常），隆基绿能 0 失败。共 37 条不通过记录，全部已按下列归因分类。**系统对真实数据稳健；失败记录全部可解释。**

### A1. 失败归因分类（已核实数值）

| 归因 | 涉及 | 数值证据 | 建议处置 |
|---|---|---|---|
| **数据源千元取整噪声**（±1000 元 vs exact 0.01 容差） | 宁德时代 4 条（IS-BAL-001/005、CF-BAL-001/004）；上汽 IS-CF-002 同因 | 宁德 IS-BAL-001：LHS 89,518,636,000 vs RHS 89,518,637,000，差恰好 1000 元；CF-BAL-001/004 同样差 1000 | 白名单（`update_manifest_whitelist.py`），原因记「数据源千元取整」。**无需改产品**——真实企业报表以元/万元为单位不存在此噪声 |
| **减值转回被 IS-LR-001 标为「列示方向异常」** | 贵州茅台、格力电器、万华化学 | 茅台 credit_impairment=+17,234,379（转回为正）；格力 credit=+5.1 亿转回、asset=-13.6 亿损失；万华 credit=+4275 万转回 | **需规则语义决策（见 B1）**——转回为正属合法列示，当前规则无法区分「转回」与「方向错误」 |
| **数据源补充资料缺「信用减值损失」行** | 万科A、中国建筑、恒瑞医药、宝钢股份、万华化学、海康威视、上汽集团（IS-CF-002） | 万科：补充资料仅 资产减值准备 219.3 亿（与 IS 资产减值精确一致），信用减值 341.7 亿无补充资料行 → LHS≠RHS | 白名单（数据源缺行）。已验证修复后的新公式行为正确（分行格式可勾稽，缺行场景保持原表现，无回归） |
| **金融企业报表格式不适用** | 工商银行、招商银行、中国平安、中信证券（各 3-4 条：BS-BAL-002/003、LR-SALES-001 等） | 金融企业资产负债表无流动/非流动分类 → 预填 0 → 合计≠总计 | **需产品决策（见 B2）**；当前白名单并注明「金融格式不适用一般企业结构规则」 |
| **经营特征真实触发 LR-* 警示** | 永辉超市（DAR/QUICK）、长江电力（QUICK）、宝钢/万华（QUICK）、海康威视（ART）、万科A/五粮液（FLUC-001）、上汽集团/中信证券（SALES-001） | 例：万科 2024 营收真实大幅下滑 → LR-FLUC-001 同比波动>30% 属**真实提示** | 逐条看数值确认后白名单，注明「经营特征真实触发，warning 级提示属预期行为」 |
| **结构性待查差异** | 上汽集团 IS-BAL-001（差 5,364 万元，非取整噪声） | RHS 合计 25,378,643,477 vs LHS 25,432,286,426，差 53,642,949 | **需人工核对**：疑似数据源存在未映射行（如净敞口套期收益/其他经营收益）或科目口径差异；查上汽利润表原文 |

### A2. 回归基线操作

- 发布门禁：`python scripts/validate_real_data.py`（茅台/格力）。**注意**该脚本硬编码 2023 年报文件名，本次以 2025 文件顶位运行（passed=26/failed=1/errored=0 两家一致）；建议把脚本改为按 manifest 驱动（见 B3）。
- 全语料：`python scripts/validate_corpus.py`——白名单化后应全绿。
- **白名单工作流**：逐条核对上表 → `python scripts/update_manifest_whitelist.py` 写入 manifest → 复跑 validate_corpus 确认 0 未白名单失败。

---

## B. 需要人工决策/核对的规则与产品问题

| # | 事项 | 背景与证据 | 建议 | 工作量 |
|---|---|---|---|---|
| B1 | **IS-LR-001 减值转回语义** | 18 家中 3 家触发，全部为合法「转回为正」；规则当前把任何正数都视为列示方向异常 | 决策：接受转回为正为合法（修改公式/加注释），或保留现状但在 notes 写明「转回场景属预期触发」。建议前者 | 半天（含准则原文确认：财会〔2019〕6 号 损失以负号填列、转回为正） |
| B2 | **金融行业报表格式适用性** | 金融 4 家的结构规则误报源于格式差异（无流动/非流动分类）；LR 阈值已有行业参数化，但 A 类结构规则无行业/格式适用性维度 | 决策：规则库增加「适用报表格式」维度（一般企业/金融企业），或文档声明系统仅适用一般企业格式（财会〔2019〕6 号） | 决策 1 小时；实现（如采纳）1-2 天 |
| B3 | **validate_real_data.py 硬编码 2023 文件名** | 本次以 2025 文件顶位才跑通；脚本文件缺失路径还有 KeyError 'name' 小 bug（:130 错误分支引用了不存在的键） | 改为读取 manifest.json 驱动（与 validate_corpus.py 对齐），顺手修 KeyError | 半天 |
| B4 | **cas_ref 条文号人工核对** | 审查发现三处引用无法离线证实：BS-BAL-001 引「基本准则(2014) 第 26 条」（该条为资产定义，恒等式出处牵强）、LR-* 引「审计准则 1313 号（财会〔2022〕36 号）」文号待证、NOTES-001 引「CAS30 第 63 条」 | 对照准则原文逐条核实并修正（resources/knowledge/ 已有 CAS 原文可本地核对） | 1-2 小时 |
| B5 | **CF-CLS 科目编码核对** | cash_flow_rules.py:36 含非标准码「2204」；:64/71「1521 其他权益工具投资」与标准科目表（1521=投资性房地产）不符——仅影响 L4 提示的漏报方向，无误报风险 | 对照贵司科目表/标准科目表修正编码 | 半小时 |
| B6 | **规则库宣传口径** | 42 条中 4 条无数据源永久跳过（BS-IS-001、NOTES-001/002、SCE-BAL-001） | 决策：规则页标注「规划中」，或宣传口径改「38 条可执行 + 4 条规划中」 | 1 小时 |

---

## C. 需要真实环境/人工验证的修复

| # | 事项 | 状态 | 需要的验证 |
|---|---|---|---|
| C1 | COM 回退行号偏移修复（excel_reader.py UsedRange 偏移） | 代码修复 + 伪对象单测已过 | **在 DLP 加密环境真实 Excel 上导入一份加密报表**，导出底稿核对科目追溯行号与原表逐位一致 |
| C2 | 打包产物冒烟 | fsa.spec 新增 resources/knowledge 入包 | 跑一次 `build_installer.ps1` → `smoke_package.ps1`；确认打包版 Agent 知识检索命中外部文档（修复前静默缺失） |
| C3 | 内部版域检查 frozen 闸门 | ✅ **已完成（2026-08-17 入域机器实测）** | 在入域机器（910373@toll.cn）上以 `-DomainWhitelist "toll.cn,TOLL"` 构建内部版：① 正常启动通过域检查进入主界面；② 设 `FSA_EDITION=general` 仍按内部版执行域检查，未被绕过；③ 代码层 `check_domain_access()` 返回 ok=True, domain=toll.cn, source=USERDNSDOMAIN |
| C4 | 视觉像素级确认 | OpenCode 已配置视觉模型（glm-4.6v），重启会话后由 agent 执行 13+ 张截图审查 | 重启后跑一轮视觉审查；重点：明暗主题对比度、CJK 裁切、skipped 卡片灰色呈现、历史页浅色渲染（审查时 OCR 坐标发现 light_history 内容区为空、dark 正常——疑似截屏时序，待像素确认） |
| C5 | SCE 合并口径修复的真实数据验证 | SCE-IS-001 已改归母派生变量（单体回退/合并取归母） | 用一份**含少数股东权益的合并年报 + SCE 表**实测（当前语料无 SCE 表）；预期：归母口径勾稽通过 |
| C6 | 真实六表包回归 | 明细勾稽口径修复（坏账净额/子级科目/前缀匹配） | 用原真实六表包复跑明细检查，确认 TB-BS-001/RC-002 不再误报且原有真实差异仍能定位 |

## D. 建议但不阻塞的事项

- import_page.py 拆分已完成（491/354/150 行三文件），后续维护注意 mixin 边界。
- `.architecture-audit/` 已加过时声明；可整个删除。
- 沙箱环境 mypy 文档命令需 `--ignore-missing-imports` 或补装 `types-openpyxl`/`pandas-stubs`/`types-xlrd`/`types-pywin32`。
- 沙箱 Python 为 3.13.9，CI 矩阵为 3.11/3.12——如需覆盖 3.13 可在 CI 矩阵加一行。

---

## E. 修复轮次留痕（备查）

| 轮次 | 内容 | 验证 |
|---|---|---|
| 修复包 1-4（审查 P0/P1 + 快修） | skipped 第四态、死设置删除+行业选择器、域检查 frozen 闸门、SCE/IS-CF-002/TB-BS-001 口径、inf 双闸、坏 PDF 包装等 | 1419 passed / ruff / mypy 全绿 |
| 修复包 5-7（延后清单） | SCE-IS-001 归母派生变量、trace 列归属、COM 行号、代际守卫、更新后台化、hover 阴影、多主体落库、文档债清理 | 1471 passed / 覆盖率 85.49% |
| import_page 拆分 | 491+354+150 三文件，纯移动零行为变化 | 1504 passed（真实语料 fixture 在场，skipif 用例全部激活） |
| 真实语料 | 18 家 A 股 2025 年报（akshare/东方财富），manifest.json 建立 | 18 家 errored=0；37 条失败全部归因（见 A1） |
