# 端到端审查报告 — 2026-08-16

> 审查依据：`REVIEW_PROMPT_E2E.md`；审查对象：当前工作树（含 fix/review-2026-08-13 未提交修改），版本 0.4.1，规则库 v1.3.0（42 条）。
> 执行方式：Phase 0 实测（pytest/ruff/mypy/覆盖率/截图）+ 四 lane 并行审查（A 财务合规 / B 工程接线（3 块）/ C 视觉 / D 交叉（2 块））+ Lane E 评分汇总。
> 证据纪律：每条发现带 file:line 或规则 ID；真实年报 fixture 缺失（合规红线），SCE/合并场景结论基于代码推演并已标注。

---

## 一、结论摘要

- **总体评级：不可发布（一票否决）→ 修复 P0 + 5 项财务 P1 后可达「内部试点可用」**
  - 一票否决项：C-P0-1（跳过规则在导入页卡片视图显示为绿色「通过」）。
  - 三大主表单体/一般企业校验内核扎实，**主表单体场景在修复 P0 后即接近内部试点**；集团合并（SCE）与明细勾稽场景须先修 5 项财务 P1。
- **四 lane 一句话总评**：
  - A 财务合规：主表规则语义正确、P1 落地架构扎实；缺陷集中在历史基线未覆盖的 SCE/合并/明细区（5 项 P1），42 条中 4 条无数据源永久跳过。
  - B 工程接线：主链路信号接线完整无断链；但设置页两组「校验参数」是纯死控件且文案谎称「即时生效」（P1），内部版域检查可被环境变量一行绕过（P1）。
  - C 视觉：令牌与组件骨架对 DESIGN_SYSTEM 几乎逐值对齐、无 Emoji；但第四态（skipped）未贯穿卡片渲染酿成 P0，负数金额红与 hover 阴影属「定义了令牌无人消费」。
  - D 交叉：离线承诺成立、数据最小化良好、打包边界干净；可追溯性在 COM 回退与 `_beginning` 列归属两处失真（P2）；种子 S5 排除为有意设计，其余定性完成。
- **发现统计：P0 × 1 ｜ P1 × 11 ｜ P2 × 23 ｜ P3 × 30+**
- **通用性红线命中：6 处**（SCE 合并口径、2019 分行减值格式、TB 总额/净额、TB 子级科目编码、附表 3 全量清单假设、附表 5 口径假设——明细六表检查整体模板锁定于特定企业格式，缺数据时 skip 保底线仍成立）

---

## 二、功能评分矩阵（Lane E；百分制：完整性 30 + 正确性证据 30 + 健壮性 20 + 易用性 20）

| # | 功能 | 完整性 | 正确性 | 健壮性 | 易用性 | 总分 | 等级 | 关键证据/扣分点 |
|---|---|---|---|---|---|---|---|---|
| 1 | Excel 导入（xlsx/xls/双栏/期间列/DLP 回退） | 28 | 27 | 17 | 17 | **89** | 生产候选 | 测试 258+ 全过、真实年报基线；扣分：主表解析异常静默吞（B1-6）、trace 在 COM 路径偏移（D2-1） |
| 2 | PDF 导入 | 26 | 24 | 14 | 15 | **79** | 内部试点 | 扣分：损坏/加密 PDF 中断整批（B4-1）、解析诊断提示死链（B1-3） |
| 3 | SCE 权益变动表 | 26 | 18 | 16 | 16 | **76** | 内部试点 | 扣分：合并口径误报 SCE-IS-001/002（A-P1-01，核心扣分） |
| 4 | 明细六表勾稽（L2/L4/附表3-6） | 27 | 17 | 15 | 16 | **75** | 内部试点 | 扣分：TB-BS-001 坏账净额+子级科目误报（A-P1-03/04）、RC-002/SAL-001/002 口径假设 |
| 5 | 规则引擎（42 条+行业阈值+自定义规则） | 26 | 24 | 18 | 16 | **84** | 内部试点 | A 类 15 条全对；扣分：4 条死规则、行业阈值 GUI 不可达（A-P1-05）、LR-GM-001 定级存疑 |
| 6 | 结果展示与追溯 | 27 | 19 | 16 | 15 | **77** | 内部试点 | 扣分：P0 skipped 显示为通过、`_beginning` 列归属错误（D2-2）、术语不一致（C-P1-2） |
| 7 | 审计底稿导出（四表+SHA256） | 28 | 26 | 18 | 17 | **89** | 生产候选 | 导出测试约 680 行强断言；扣分：COM 路径行号偏移（D2-1） |
| 8 | Agent 诊断（LLM+辩论+兜底+知识库） | 26 | 22 | 17 | 16 | **81** | 内部试点 | 离线守卫/数据最小化/兜底链路优秀；扣分：打包版知识库静默缺失（B6-2） |
| 9 | 历史与持久化 | 27 | 26 | 17 | 17 | **87** | 生产候选 | DDL 幂等、单事务 1+N、降级判空；扣分：降级时「存储不可用」与「无记录」不可区分（B3-5） |
| 10 | 设置与容差覆写 | 20 | 18 | 12 | 10 | **60** | **演示级** | 两个死设置+「即时生效」误导文案（P1×2）、恢复默认不清覆写、启停不持久化、负容差异常逃逸 |
| 11 | 自动更新 | 25 | 24 | 13 | 13 | **75** | 内部试点 | install 契约正确、SHA256 尽力而为合理；扣分：检查/下载主线程冻结（B5-1） |
| 12 | 多主体批量校验 | 22 | 22 | 15 | 14 | **73** | 内部试点 | ICF-002 双边核对有测试；扣分：结果不落库不入历史（B1-5）、无 GUI 实体配置入口 |
| 13 | 打包与发布（安装器/签名/冒烟/双版本） | 27 | 24 | 12 | 15 | **78** | 内部试点 | CI 打包链路完整；扣分：域检查环境变量绕过（B6-1，核心扣分）、知识库未入包 |

**整体判定逻辑**：存在 P0 → 一票否决「不可发布」。P0 修复后：核心功能 1/5/7/9 均 ≥84，无其他 P0 → **内部试点可用**；面向集团合并场景对外交付须先修 A-P1-01/02/03/04。

---

## 三、P0 / P1 必须修复清单（按级别与影响排序）

| # | 级别 | 维度 | 标题 | 证据 | 影响 | 最小修复建议 |
|---|---|---|---|---|---|---|
| 1 | **P0** | 视觉/结果展示 | 跳过规则在导入页卡片渲染为绿色「通过」，skip 原因永不显示 | `result_card.py:200-203`（`_get_status_key` 忽略 `skipped`）+ `result.py:164-182`（`from_skip` → `passed=True`）+ `result_card.py:180`（message 因 `not passed` 永不显示）；审计页 `audit_page.py:171-177` 显示「跳过」——两处结论矛盾 | 缺数据未校验的规则被呈现为「通过」= 视觉误报通过，直接违反 P1 原则；本环境 synthetic fixture 即有 17 条 skipped，用户会看到 17 张绿色「通过」卡 | `_get_status_key` 增加 skipped 分支（灰底中性色+文案「跳过」），详情区对 skipped 展示 message |
| 2 | **P1** | 财务合规 | SCE-IS-001/002 合并口径错位：SCE 未分配利润/OCI 列是归母口径，对比的 `net_profit`/`other_comprehensive_income_after_tax` 是含少数股东合计数 | 规则公式（exact 0.01）+ `sce_extractor.py:44-48`（不提取少数股东列）+ `name_mapper.py:130/207` | 合并报表+少数股东损益≠0 时必然误报不通过；本系统目标场景（集团六表包）高发 | ① 短期降 warning + notes 注明口径差异；② 公式改比 `net_profit_parent`（已映射，单体缺行按 P1 跳过）；③ 提取器纳入少数股东列后公式求和 |
| 3 | **P1** | 财务合规 | IS-CF-002 漏用「信用减值损失」补充资料行 | 公式 `cf_notes_impairment == -credit_impairment - asset_impairment`；`name_mapper.py:228-229` 定义了 `cf_notes_credit_impairment` 但**无规则消费**（grep 证实） | 2019 分行列示格式的企业（制造业坏账为信用减值主项）必误报 | 导入侧合成 `cf_notes_impairment_total`（两行合计，缺行按 0）；修复后用真实年报回归复验 |
| 4 | **P1** | 财务合规 | TB-BS-001 默认映射「应收账款总额 vs 报表净额」 | `detail_validation_service.py:31-35`（codes 仅 `("1122",)`）+ `detail_checks.py:254-256`（借−贷总额）vs BS 应收账款净额（扣 1231 坏账准备） | 坏账准备≠0 的企业（常态）必误报 ERROR | 默认 codes 改 `("1122","1231")`（贷方备抵自然净额化，机制已支持）；或降 WARNING |
| 5 | **P1** | 财务合规 | TB-BS-001 科目编码精确匹配，子级科目余额表 expected=0 误报 | `detail_checks.py:248-256`（`not in codes` 精确匹配）+ `detail_parsers.py:29-43`（无向上归卷）；序时账侧却用 `startswith`（:160-161），同库两套口径 | 金蝶/用友末级科目导出格式（常见）三项默认映射全误报 ERROR | 改「前缀匹配 + 父级行有子级则跳过」；短期文档明示按末级编码配置 |
| 6 | **P1** | 财务合规/接线 | 行业阈值参数化对 GUI 用户完全不可达 | `import_page.py:637-642` 不传 `threshold_vars`（全库仅无 GUI 消费方的 `multi_entity_service.py:113-117` 传递）；GUI 无行业选择入口 | 财务人员配了行业仍按 general 阈值执行（如建筑业应收/营收 30% vs 60%），用户认知与实际行为背离 | import_page 校验时注入 `EntityConfig.threshold_vars()`；或文档言明「单体仅 general」 |
| 7 | **P1** | 工程/设置 | 设置页「默认容差（元）」死控件，文案谎称「即时生效」 | 写入 `settings_page.py:166-184` → `AppState.default_tolerance`（app_state.py:126-129）**全库无消费方**（grep 确认）；引擎容差=规则 JSON+规则页覆写；文案 `settings_sections.py:150`「即时生效」 | 财务用户改容差校验结果不变——直接动摇「校验参数生效」的核心预期 | 接通：把 default_tolerance 注入 `PackageValidationService`/`DetailCheckConfig`；或删除控件。先改文案止血 |
| 8 | **P1** | 工程/设置 | 设置页「毛利率波动阈值」死控件 | `settings_page.py:186-188` 写 QSettings `gross_margin_threshold`，无任何读取方；与引擎 `gm_yoy_threshold`（thresholds.py:52-55，比例 0.30）量纲都不一致（30 vs 0.30） | 同上，且即使接通也存在 100 倍量纲错位 | 删除控件，或接通并统一为比例量纲（0.30） |
| 9 | **P1** | 打包/授权 | 内部版域检查可被环境变量完全绕过 | `edition.py:58-60`：`FSA_EDITION` 环境变量优先级**高于**构建期固化 `_edition_override`——冻结内部版 `set FSA_EDITION=general` 即跳过全部域检查；`FSA_DOMAIN_WHITELIST`(:108)、`USERDNSDOMAIN`（用户可伪造）同理 | 内部版唯一授权闸门失效 | frozen 模式（`getattr(sys,"frozen",False)`）忽略三个环境变量，仅认 `_edition_override`；中长期域判定改 WMI/注册表（`Win32_ComputerSystem.PartOfDomain`） |
| 10 | **P1** | 视觉/术语 | 负数金额红定义了令牌但全 GUI 无人消费 | `AMOUNT_NEGATIVE` 入令牌（theme.py:44/95/123）但 grep 无消费方；差额列/科目金额列/trace 金额均不设负数色 | DESIGN_SYSTEM §2.2/§7.4 落空；财务用户看负数无红色提示 | 金额渲染处 `< 0` 时 `setForeground(amount_negative)`（差额列/trace/科目清单三处） |
| 11 | **P1** | 视觉/术语 | 状态术语体系不一致 | 汇总卡「错误」（import_page_results.py:148）vs 卡片「不通过」（result_card.py:32）；警告卡副文案「建议关注的异常」把「异常」误用于 warning，与 errored=异常 冲突 | 财务术语混乱（P4/P6），「异常」一词两个含义 | 统一四词「不通过/警告/异常/跳过」；副文案改「建议关注的偏差」 |
| 12 | **P1** | 测试/工程 | 新分支测试 `test_logging.py::test_linux_uses_home_dot_fsa` 在 Windows 必挂 | 本分支实测：mock 了 `sys.platform` 与 HOME，但 Windows 上 `Path.home()` 走 USERPROFILE 语义，被删环境变量后抛 `RuntimeError: Could not determine home directory`（tests/core/test_logging.py:19 → core/logging.py:25） | 合并后 CI windows job 必挂，主流程测试门禁失效 | 测试改为 mock `Path.home` 或 `expanduser`；Windows 下 skip 亦可但会损失覆盖 |

---

## 四、分维度详细发现（P2/P3 浓缩；完整证据链见各 lane 工作底稿）

### A. 财务合规（P2×5 / P3×9）

- **A-P2-01 LR-GM-001 负毛利判 ERROR**：真实经营状态（清仓/困境/生鲜）被标「必须修正的差额」，与同族 LR-GM-002（warning）不一致——建议降 warning（设计意图待确认）。
- **A-P2-02 四条规则无数据源永久跳过**（BS-IS-001 / NOTES-001 / NOTES-002 / SCE-BAL-001）：`dividends/surplus_withheld/prior_period_adjust` 全库无来源（grep 证实），NOTES 两条为模板变量，SCE-BAL-001 自认占位——**宣传口径应修正为「38 条可执行」**或在规则页标注「规划中」。
- **A-P2-03 SAL-001 成本构成部分填报误报**（supplementary_checks.py:100 仅「全空」跳过，部分填报无法区分「未填」与「真为 0」）。
- **A-P2-04 RC-002 坏账口径差异报错而非提示**（reclassification_checks.py:183-208：message 承认「差异可能来自坏账准备」但判定仍是 fail/ERROR）。
- **A-P2-05 SAL-002 明细合计 vs 利润表全量收入口径假设**（附表 5 仅覆盖主营时误报 ERROR）。
- **P3**：LR-ART-001（0.2 vs 实际 0.3）/LR-DAR-001（0.7 vs 实际 0.85）default_tolerance 陈旧显示值；导入页「错误」卡片合并 errored 且副标题「必须修正的差额」不精确（import_page_results.py:132-140）；LR-QUICK-001 名为速动实为流动比率；CF-CLS-001 含非标准码 2204、CF-CLS-005/006「1521 其他权益工具投资」与标准科目表（1521=投资性房地产）不符（仅漏报方向）；个别 cas_ref 条号待人工核对；unmapped_names 仅 Agent/日志可见、导入页无提示；merge_summaries 与 ValidationService 的 total 语义不一致（含不含 skipped）；CF-DTL-001 同名项目双向行只取一侧；ICF-002 对方主体名精确匹配静默漏配。

**42 条体检结论**：A 类 15 条全部语义正确；B 类 14 条中可靠 8、缺陷 4（本报告 P1-2/3）、死规则 3；C 类 13 条守卫齐备无除零路径。`pytest tests/rules tests/engine` 258 全过。**分层可信度：三大主表单体可直接采信；SCE/合并/明细修复前不可采信；未分配利润滚动与附注勾稽能力不存在。**

### B. 工程接线（P2×10 / P3×10+；控件清单全量见 B-1 底稿）

**主链路逐环验证：信号接线完整无断链**（import→validate→merge→AppState→各页→导出→持久化，全部消费方接线确认）。重点 P2：

- **B1-1 后台导入/校验无代际守卫**：进行中点「重置」，完成后旧结果写回 AppState（import_page.py:677-680/522-530；取消仅在固定检查点生效 :634/:651）。
- **B1-2 重新导入不清理旧校验结果**（import_page.py:480-481 无 `set_results` 清空）——旧底稿仍可导出，SHA256 留痕与新报表不匹配（设计意图待确认）。
- **B1-3 PDF 解析诊断提示死链**：GUI 走 `import_page.py:420` 直接 `read_pdf`，`parse_diagnostics` 恒空（importer.py:60-64 仅 `import_file` 设置）——「PDF 只读扫描」引导提示在 GUI 永不出现。
- **B1-6/S2 主表/明细导入异常静默吞**（import_page.py:438-446 仅 logger.debug、不进 errors），文件仍计入「成功导入 N 个文件」。
- **B4-1 损坏/加密 PDF 中断整批**：pdfminer 的 `PDFSyntaxError`/`PDFPasswordIncorrect` 不在 import_page.py:427 捕获族 → 逃出后被 :380 兜底，整批失败（违背单文件隔离承诺，测试仅覆盖 Excel 路径）。
- **B5-1/B1-7 更新检查/下载主线程同步**：检查冻结最长 10s；137MB 安装包下载全程冻结无进度（settings_page.py:337/379；启动检查的 `_UpdateCheckBridge` 后台模式未复用）。
- **B3-2 恢复默认设置不清规则覆写**（override_repo.clear() 全库无调用方，提示语「校验参数已恢复默认」与事实不符）；**B3-2b 规则启停不持久化**（registry._disabled 仅内存；rule_overrides 表无 enabled 列；storage/AGENTS.md 声称支持——文档与实现不符）；**B3-3 规则页负数/NaN 容差异常逃逸 slot**（输入 -1 无反馈、显示与状态分叉；「nan」会使该规则全部走 Decimal InvalidOperation → 异常）。
- **B6-2/S4 打包版知识库静默缺失**：fsa.spec datas 无 resources/knowledge（约 590KB 公开准则文本，无体积/许可障碍），冻结版 Agent 检索静默变薄——定性为**缺陷而非发布策略**（无文档化排除理由+降级不可感知）。
- P3 要点：后台线程顶层宽 catch 不记堆栈（:380/:616/:714 补 logger.exception）；theme.py:982 无日志吞异常；未设 threading.excepthook；closeEvent 未取消 _followup_worker 与导入/校验任务（实际风险低）；gui/AGENTS.md 线程约定措辞已过时（「校验/导入/导出仍主线程同步执行」与事实相反）；覆盖率口径不一致（pyproject fail_under=90 vs 实际 84.73% vs CI 84——文档本地命令必失败）；multi_entity_dialog.py 0% 覆盖、agent_sessions.py 42%。

**控件行为清单结论**：五页+侧栏+顶栏+AI 抽屉+对话框全量控件已逐个点验，绝大多数接线正常；「死控件/半死控件」共 8 个，其中 2 个 P1（设置页默认容差、毛利率阈值）、2 个 P2（规则启停不持久化、容差负数无防护）、4 个 P3（重试按钮重置后残留、HistoryCard total 死参数、DropZone 冗余信号、历史搜索无匹配复用空态文案）。

### C. 视觉（代码级审查；像素级项目因无可用识图通道标「未验证」）

除 P0-1 与 P1-1/P1-2（见第三节）外：
- **P2**：「异常/跳过」无独立筛选标签且筛选桶与卡片色不一致（errored 并入「错误」桶但卡片渲染黄色「异常」）；report_card 科目清单金额列未右对齐未用等宽字体（report_card.py:168-178，违反 §7.4）；hover 阴影（呼吸感）未实现且 `get_shadow_color` 死代码；AgentDrawer MIN_WIDTH=320 与规范/自身注释 280px 矛盾。
- **P3**：硬编码 hex 四处（agent_drawer 停止按钮 rgba(239,68,68) 深色下应用 #f87171、免责灰、忙碌条、agent_messages 回退 #d0d3d9）；历史页统计数字 20px/700 脱字号阶梯；ResultCard 首渲染缺字重（`_apply_status_style` 未在 `__init__` 调用）；长规则名截断无 elide/ToolTip；FAB 样式 QSS/内联双源；PageTitle 15px vs 规范 22px。
- **合规确认（勿动）**：明暗双色板与 DESIGN_SYSTEM §11 逐值一致；结果卡片浅色语义填充+8px 圆角+默认无阴影；汇总卡 22px/600 深色大数字+色点；FAB 纯色 brand-600 无渐变；侧栏激活态 3px 指示条；无 Emoji、图标全量 FluentIcon；金额千分位+等宽字体主线覆盖；高 DPI RoundPreferFloor。
- **视觉完成度评价**：配色与组件骨架已达规范，**状态语义闭环未完成**（第四态 skipped 未贯穿卡片与筛选层）。
- **未验证→已补验**：识图通道已于当日下午开通（multimodal-looker 切换至 zhipu/glm-4.6v，实测可识图），像素级审查结果见文末「视觉审查补篇」。此前 OCR 坐标发现的明暗历史页差异已查清为**截屏时序假象**（补验截图两主题均有内容）。

---

## 视觉审查补篇（2026-08-16 下午，识图通道：glm-4.6v，31+3 张截图）

> 执行方式：13 张标准截图 + 18 张扩展交互状态截图（scripts/ux_shots_extended.py 新增）+ 3 张沉淀补拍；7 个并行批次 + 3 轮定向终审；所有结论经 ground-truth 校准（侧栏文字/亮度测量/代码交叉验证）。

### 高置信验证通过项

- **P0 修复视觉确认**：跳过卡片为灰色「跳过」态、展开可见跳过原因消息、筛选标签为六态（全部/不通过/异常/警告/跳过/通过）、「不通过」筛选仅显示红色失败卡片——五项专项验证全过。
- 浅色主流程五页：布局/配色/图标/金额格式无 P1 发现；侧栏激活态、语义色、线性图标正确。
- 深色主题：深色抽屉无浅色残留、文字可读、建议气泡可见；历史/审计/规则/设置页深色正常。
- AI 抽屉空状态：建议问题气泡 + 引导文案均在（首轮误报「缺失」已驳回）。
- LLM 不可用兜底：中文友好提示 + 规则化答复仍提供，无技术黑话/异常栈。
- AI 免责声明：持久底栏存在（agent_drawer.py:262「AI 输出仅供参考 · 不构成审计意见 · 请以规则引擎与人工复核为准」），为设计有意的半透明小字——P1 合规。

### 真实视觉发现（新增，待处理）

| # | 级别 | 发现 | 证据/置信度 |
|---|---|---|---|
| V1 | P2 | AI 抽屉与主内容区层次边界不清（明暗主题均有此感），建议加左侧 1px 分隔线或轻阴影 | 批 3 双主题一致报告；中置信 |
| V2 | P3 | 抽屉建议气泡行距偏密 | 批 3；中置信 |
| V3 | P3 | 空状态视觉引导单薄：导入页拖放区仅文案（无图标/箭头），审计页空态无「去导入」下一步按钮 | 批 6；中置信 |
| V4 | P3 | 导入失败 InfoBar 文案过长且语义重复 | 批 6；中置信 |
| V5 | P3 | 历史页时间格式可读性（建议相对时间）、规则页搜索无结果计数提示 | 批 6；中置信 |

### 驳回的误报（防止误改）

- 「对话框直角边框」——offscreen 截屏无 DWM 圆角，环境假象（真机 Windows 11 原生圆角）。
- 「科目清单金额列未右对齐」——误读；report_card.py:185-190 已有 AlignRight+等宽+负数红（修复包 1）。
- 「trace 表金额未等宽右对齐」——与代码（result_card.py:310）冲突，倾向误读，人工过目即可。
- 「深色版 dark_import.png 是浅色」——截屏时序假象（主题切换 200ms 遮罩淡出，脚本未沉淀）；**非应用缺陷**；ux_shots.py/ux_shots_extended.py 已补沉淀等待。

### 易用性评分影响（Lane E 修订）

视觉补验后无新增 P0/P1 级视觉缺陷。「结果展示与追溯」易用性维度由 15/20 上调至 17/20（P0 已修且视觉确认，扣分为术语/筛选细节已于修复包 1 处理）；「Agent 诊断」易用性 16/20 → 17/20（空状态/兜底/免责全部确认）。整体评级维持「修复 P0+P1 后内部试点可用」不变——该条件已满足，**现评级：内部试点可用**（待 C1-C6 真实环境验证后评估生产可用）。

### 遗留

- V1-V5 建议列入下一迭代 UI 打磨。—— **已于当日修复完毕（见终审）**
- 深色输入框/开关的视觉权重（批 2 发现）建立在过渡态截图上，置信度低，建议真机过目确认后再决定是否处理。
- 像素级最终确认仍建议真机人工过目一轮（offscreen 截屏无法覆盖真实 DPI 缩放与系统圆角）。

---

## 终审：修复后模块可用度与产品化评分（2026-08-16 晚）

> 前置：P0×1 + P1×11 + P2/P3 约 45 项已全部修复并测试验证；识图审查确认关键修复；18 家真实年报语料回归 errored=0；打包+冒烟实测通过。
> 终审门禁：ruff 全过；mypy 98 文件零错误；pytest **1537 passed / 0 failed**；覆盖率 **86.29%**。

### 模块评分表（修复后；百分制，对比审查时初评）

| 模块 | 初评 | 终审 | 可用度判定 | 关键变化 |
|---|---|---|---|---|
| Excel 导入 | 89 | **92** | 生产候选 | 静默吞错已升日志+提示链修复；真实 18 家回归通过 |
| PDF 导入 | 79 | **84** | 内部试点 | 坏/加密 PDF 中文包装；诊断提示链接通 |
| SCE 权益变动表 | 76 | **84** | 内部试点 | SCE-IS-001 归母派生变量彻底修复；cas_ref 修正（35-36 条） |
| 明细六表勾稽 | 75 | **82** | 内部试点 | 坏账净额化+子级科目前缀匹配；三条口径检查降 WARNING |
| 规则引擎（42 条） | 84 | **88** | 生产候选 | cas_ref 联网核实修正；IS-LR-001 转回语义；死规则标注「规划中」；CF-CLS 编码修正 |
| 结果展示与追溯 | 77 | **88** | 生产候选 | P0 第四态修复+视觉确认；_beginning 列归属修正；六态筛选；术语统一 |
| 审计底稿导出 | 89 | **90** | 生产候选 | 四表+SHA256+行号口径说明；exporter 测试强覆盖 |
| Agent 诊断（无 LLM 链路） | 81 | **88** | 生产候选 | 42 条规则离线解答全覆盖（diagnosis 4 族缺口补齐+知识库 2 新条目）；打包版知识库已入包 |
| 历史与持久化 | 87 | **89** | 生产候选 | 时间格式化；存储降级文案区分 |
| 设置与容差覆写 | 60 | **85** | 内部试点 | 死设置删除+行业选择器接通+恢复默认清覆写+负数守卫；残留：规则启停不持久化（P2 未修） |
| 自动更新 | 75 | **85** | 内部试点 | 检查/下载后台化+进度显示；install 契约验证正确 |
| 多主体批量 | 73 | **80** | 内部试点 | 结果落库+对话框提示；残留：无 GUI 实体配置入口 |
| 打包与发布 | 78 | **88** | 生产候选 | **打包脚本 BOM 解析失败的真实 P1 修复**；构建（333MB）+冒烟实测通过；域检查 frozen 闸门 |

### 产品化总评

- **当前状态：内部试点可用偏上（接近生产候选）**。三大主表单体校验 + 导出 + 历史 + 离线 Agent 全链路在真实 18 家语料上零异常；视觉系统经识图审查确认。
- **距「生产可用」还差**：① C1/C3 真实环境验证（DLP 加密机实测 COM 行号、入域机实测内部版闸门）；② C5 含少数股东权益的合并+SCE 真实数据复验；③ 两个漏修 P2（主表/明细导入失败的 debug 级静默应升入用户可见提示；规则启停持久化）；④ 真机人工过目一轮 UI（offscreen 截屏不能替代真实 DPI/圆角）；⑤ 发布前把 validate_real_data.py（已 manifest 驱动）挂入发布 checklist。
- **数据边界提示（产品文档需写明）**：系统适用财会〔2019〕6 号一般企业格式；金融企业格式暂不支持（语料实测结构性误报已白名单隔离）；千元取整数据源（东财类）会触发 exact 容差提示，属数据源精度而非报表错误。

### D. 交叉（P2×4 / P3×9；种子定性见第六节）

- **D1-1（P2）`parse_amount` 不拒绝 inf**：单元格「1e999」→ inf 流入引擎 → `compare(inf, inf)` 抛 `decimal.InvalidOperation` → 兜底为「未预期错误： ...」英文技术消息（违反 P4）。修复：amount_parser 与 comparator 加 `math.isinf` 检查。
- **D2-1（P2）COM 回退路径 trace 行号偏移**：`excel_reader.py:472` 用 `UsedRange.Value`，UsedRange 左上角不保证 A1——DLP 加密环境（COM 是唯一路径）下底稿行号系统性偏小。
- **D2-2（P2）`_beginning` 变量 trace 列归属错误**：`runner.py:334-347` 剥后缀后取 `item.column`（=期末列头），期初金额实际来自 secondary 列——底稿中 `xxx_beginning` 行显示「期末余额」列。
- **D3-1（P2）真实年报最强证据在 CI 不可复验**（合规红线使然，建议把 `validate_real_data.py` 挂进发布 checklist 强制执行）。
- **其余 P3**：`_to_decimal` 的 except 回退死代码；1e14 预警边界 `>` vs `>=`；PDF「表内第 N 行」含表头行（口径需知晓）；conftest 工厂无 `beginning_amount` 参数致双列路径覆盖弱；问题包提示文案漏了「日志也含文件全路径」；`llm_api_key` 明文存注册表；assistant 历史消息回灌不再消毒（低危）。
- **正面确认**：Decimal 比较对科学计数法/大数实测正确（1e15 精确解析）；「空 vs 0」语义无反向漏报（空→None→skip，占位符→0.0 符合会计惯例）；Excel/PDF/SCE/期间列的 trace 行号主线逐位准确且有专测；engine/exporter/services 测试断言的是财务结论而非「不抛异常」；离线承诺成立（网络面仅 updater+agent，urllib only）；LLM 数据最小化落地良好；打包边界干净（无 API key、无 fixture、无开发产物入包）。

---

## 五、通用性硬编码专项清单（换一家企业就可能出错的点）

1. **SCE-IS-001/002 合并口径**（P1-2）：单体正确，合并+少数股东≠0 必误报。
2. **IS-CF-002 减值补充资料格式假设**（P1-3）：2019 分行列示格式失效。
3. **TB-BS-001 总额/净额 + 精确编码**（P1-4/5）：默认映射对企业 TB 格式（净额/末级科目）强假设。
4. **RC-002/SAL-002 附表全量清单假设**（A-P2-04/05）：附表 3/5 非全量时误报。
5. **明细六表整体模板锁定**（附表 4/5/6 列结构为公司特定模板；缺数据时 skip 保底成立，但检查名「附表 4/5/6」易被误读为通用标准）。
6. **ICF-002 对方主体名与文件夹名精确相等假设**（multi_entity_service.py:283，静默漏配）。

## 六、已知问题种子核查结论（S1-S7）

| 种子 | 结论 | 定级 |
|---|---|---|
| S1 LSP 类型错误（excel_reader ModuleType 等） | **排除**——CI 口径 mypy（--ignore-missing-imports, py3.12）96 文件全干净；LSP 报错系分析器口径差异假象。附带发现：文档本地命令 `mypy src/fsa` 在缺 stubs 环境会报 31 个 import-untyped（环境问题，P3） | 排除 |
| S2 import_page 五处宽 catch | **部分确认**：:438/:444 静默吞错为缺陷（P2，见 B1-6）；:380/:616/:714 线程边界兜底合理但丢堆栈（P3） | P2+P3 |
| S3 excel_reader.py:419 gui/ 外 type: ignore | **确认**（运行时无风险）：用 `cast()` 一行替换即可消除 | P3 |
| S4 打包版无外部知识文档 | **部分确认 → 定性为缺陷**（无文档化排除理由 + 降级不可感知）：datas 加一行或冻结模式告警 | P2 |
| S5 compute_sha256 双实现 | **排除**——有意设计且已文档化（审计留痕尽力而为 vs 更新校验必须硬失败，语义分化有据）。残留观察：sha256_file 返回 "" 会被无条件写进底稿留痕（P3） | 排除 |
| S6 tests/gui 无 __init__.py | **确认存在，当前无影响**（无重名冲突，PEP 420 命名空间包可工作）；补空 `__init__.py` 消除整类风险 | P3 |
| S7 根目录 custom_rules.json | **确认——运行时产物误入 git**（89ae3d1 混入的空占位 `{"rules": []}`，每次运行被重写）：`git rm --cached` + gitignore | P3 |
| 新增 | **test_logging.py Windows 必挂**（编排者 Phase 0 实测确认，本分支引入） | **P1** |

## 七、观察到的良好实践（勿动）

- **P1 落地架构**：EvaluationError→skip / FormulaParseError→errored 异常阶梯 + KNOWN_LINE_ITEM_KEYS 排除清单逐项 NOTE 论证——全库未发现「缺数据报错而非跳过」的漏网。
- **Decimal 域容差比较** + RelativeBaseZeroError 归入 skip；科学计数法/大数实测正确。
- **行业阈值注入的审计追溯**（INDUSTRY_THRESHOLD_RULES + trace 展示实际注入值）。
- **明细检查的保守降级**：CF-DTL-001 未匹配明细降级 skipped+WARNING；缺主表明细检查整条 skip；L4 现金流分类全 WARNING。
- **异常区分四处正确**：审计页/结果卡片/导出底稿/历史筛选对 errored/skipped 的区分均正确（唯导入页汇总卡片合并，P3）。
- **离线安全面**：远程 LLM 默认阻断 + http 明文警告 + 输入消毒 + 工具结果截断 + 路径脱敏。
- **持久化**：DDL 幂等迁移、单事务 1+N 写入、存储降级不崩溃。
- **updater install() 三秒延迟契约被调用方正确遵守**；SHA256「取得必验、缺失不阻塞」语义合理。

## 八、附录：Phase 0 实测记录

| 命令 | 结果 |
|---|---|
| `python -m pytest -q` | **1425 passed / 1 failed / 1 skipped / 22 deselected(slow)**，60s；唯一失败 = test_logging.py::test_linux_uses_home_dot_fsa（Windows 平台 bug，本分支引入） |
| `python -m ruff check src/ tests/` | All checks passed |
| `python -m mypy --no-incremental --ignore-missing-imports --python-version 3.12 src/fsa`（CI 口径） | Success: no issues found in 96 source files |
| `python -m mypy src/fsa`（文档本地口径） | 31 errors 全部 import-untyped（缺 types-openpyxl/pandas-stubs 等，环境问题） |
| 覆盖率 | 整体 84.73%（pyproject fail_under=90 本地 FAIL；CI 84 基线过线）；0% 文件：gui/widgets/multi_entity_dialog.py；低覆盖：agent_sessions 42%/drop_zone 62%/theme 66%/settings_page 68% |
| `python scripts/ux_shots.py` | 13 张截图生成成功（realistic_report.xlsx 顶位茅台路径；校验 25 跑/23 过/2 不通过/17 跳过——不通过为 fixture 数据本身不平，非系统缺陷） |

**审查局限声明**：① 真实年报 fixture 缺失，SCE/合并/明细的误报结论基于准则格式与代码推演，修复后须以 `scripts/validate_real_data.py` 真实数据回归确认；② 像素级视觉审查因识图通道全部不可用而未执行（详见第四节 C）；③ 本次审查未实跑打包产物（smoke_package.ps1 未执行）。
