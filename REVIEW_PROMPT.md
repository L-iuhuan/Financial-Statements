# 财务报表勾稽校验系统 — 全面审查 Prompt

> **归档说明 (2026-08-15)**: 本文为历史设计/过程文档，其中规则数量、目录结构、依赖清单等具体数值可能与现行代码不一致；一切以根 AGENTS.md 与代码为准。


> 用途：将本文件作为任务指令交给审查者（人或 AI Agent），对本项目执行一次端到端深度审查。
> 审查对象：当前代码库全量（`src/fsa/`、`tests/`、`cas_gouji_rule_library.json`、`scripts/`、打包配置）。
> 本 Prompt 自包含：审查者无需任何外部上下文即可开工。

---

## 0. 审查纪律（先于一切角色）

1. **只报告，不改代码。** 审查产出是一份报告；修复建议可以写，但不落地改动。
2. **每条发现必须有证据**：文件路径 + 行号，或规则 ID + 公式原文。没有证据的"感觉有问题"不写入报告。
3. **严重级别统一定义**：
   - **P0 阻断**：财务结论可能错误、数据可能丢失/污染、应用崩溃、合规性硬伤。
   - **P1 高**：特定场景下产生错误结果或误导性提示、核心流程断链。
   - **P2 中**：边界场景缺陷、体验明显受损、性能问题。
   - **P3 低**：规范偏离、可维护性、轻微体验瑕疵。
4. **区分"缺陷"与"设计权衡"**：项目有明确的保守设计原则（见 §1），审查时先判断某行为是否是有意为之，再定级。疑似权衡的标注"（设计意图待确认）"。
5. **通用性红线（重点）**：任何"只在当前测试数据 / 当前企业 / 当前机器上成立"的逻辑都是审查目标。包括但不限于：
   - 生产代码中引用测试夹具、演示数据、特定企业科目编码（如写死的 1002/1012/6111 等会计科目）；
   - 针对某家公司报表格式的硬编码（固定行列号、固定表头文本、固定工作表名）；
   - 环境特定值（公司内网地址、特定 LLM 端点默认值、本机路径）泄漏进通用逻辑；
   - 魔法数字（容差、阈值、行数）无出处、不可配置。
   判断标准：换一家不同行业、不同格式的企业报表，该逻辑是否仍然成立。
6. **输出全中文**，面向项目负责人（财务+技术双语读者），技术术语允许但要给出业务含义。

---

## 1. 背景速览（审查者必读）

- 本项目是**离线 Windows 桌面软件**：导入企业财务报表（Excel/PDF），按 CAS 规则库执行勾稽校验，输出差异报告与审计底稿。
- 规则库：`cas_gouji_rule_library.json`（v1.2.0，37 条），分 A-表内平衡 / B-表间勾稽 / C-逻辑合理性三类。
- 项目六条强约束原则（审查时作为"设计意图"基准）：
  - **P1 宁可漏报不可误报**——缺数据时规则应**跳过**而非报错；容差偏向通过。
  - **P2 确定性**——同输入同输出，不依赖随机/时间/网络。
  - **P3 可审计可溯源**——每条结果可追溯到科目 + 原始行列（`TraceItem`）。
  - **P4/P6 面向财务用户 + 全中文**——错误信息讲"哪里错"，不抛技术异常。
  - **P5 模块隔离**——引擎只认 `Report` 对象，不知报表来源；GUI 不直接碰 SQLite。
- 关键路径：`core/engine/`（规则求值）、`core/importer/`（Excel/PDF/明细导入）、`services/`（校验编排）、`agent/`（LLM 诊断）、`storage/`（SQLite WAL）、`gui/`（PySide6 五页 + AI 抽屉）。
- 已知运行环境：公司 DLP 透明加密（Excel 需 COM 回退读取）、国内网络（推送走 SSH）、可选本地/在线 LLM。

---

## 2. 角色 A — 财务专家审查（最高优先级：合规性）

> 立场：签字会计师。你要为"这份校验结果能不能拿去用"负责。
> 财务发现优先于一切技术与设计发现，报告中置前。

### A1. 勾稽规则的正确性（逐条过 37 条）

- 逐条核对 `cas_gouji_rule_library.json` 中每条规则的**公式语义**是否符合准则原文：
  - 会计恒等式与表内平衡（BS-BAL/IS-BAL/CF-BAL 系列）是否与 CAS 30 列报格式一致；利润表是否为 CAS 2019 多步式（营业利润 = 营业总收 − 营业总成本 + 其他收益 + 投资收益 + 公允价值变动 + 信用减值 + 资产减值 + 资产处置收益），注意各减值科目在公式中的**符号方向**（损失以负号列示 vs 正号加回）。
  - 现金流量表直接法与间接法（补充资料 `cf_notes_` 前缀变量）勾稽是否成立（CF-BAL-002/003、IS-CF-001/002）。
  - 权益变动表（SCE 系列）勾稽口径是否正确，`sce_` 变量映射是否对应正确的矩阵位置。
- 核对 `cas_ref` 引用的准则条款（CAS 14/18/30/31/33、审计准则 1313 号）**是否真实存在且引用对题**，不允许虚构或张冠李戴。
- 核对容差类型与业务语义匹配：`exact 0.01`（平衡关系）、`relative`（同比波动）、`threshold`（区间判断）是否用对；BS-BAL-004 用 0.1% 相对容差是否有充分理由。

### A2. P1 原则落地核查（误报风险排查）

- 找出所有"数据缺失时可能报错而非跳过"的路径：变量未定义、除零、期初缺失、单体报表遇到合并专属变量（少数股东权益、归属于母公司）等。重点核查 SCE-BAL-002、BS-IS-001 这类已知有适用性条件的规则，确认跳过逻辑真的生效。
- 检查 `errored`（执行异常）与"不通过"（真实差异）是否在结果、统计、UI 文案上严格区分——把"缺数据"显示成"不通过"即是 P1 级误报。
- 逻辑合理性规则（LR-* 系列）的触发条件是否有足够的保守性（如 `revenue <= 0 or ...` 的前置守卫），是否存在对正常经营企业的误报（如负毛利企业、金融/地产行业的资产负债率）。

### A3. 科目映射的财务正确性

- 审查 `core/importer/name_mapper.py`：中文科目名 → snake_case key 的映射是否会计口径正确；重点抽查易混科目（其他综合收益 vs 综合收益总额、信用减值损失符号、库存股减项、少数股东权益）。
- 名称清洗规则（括号注释剥离、后缀清理）是否存在**误伤**：把两个不同科目清洗成同一 key 会导致取错数。
- 映射缺失时的行为：未知科目是丢弃、保留还是报错？丢弃是否静默（P3 可审计性受损）？

### A4. 明细勾稽的财务逻辑（L2/L4/附表 3-6）

- `core/engine/detail_checks.py`、`cash_flow_checks.py`、`reclassification_checks.py`、`supplementary_checks.py` 逐条核对：
  - 凭证借贷平衡、现金流明细 ↔ 主表、现金流明细 ↔ 序时账（现金等价物口径）、余额表 ↔ 资产负债表的勾稽口径是否财务上成立；
  - 往来重分类（负数重分类、与报表勾稽）是否符合"调表不调账"实务；坏账准备口径差异是否被正确识别为"口径差异提示"而非"错误"；
  - 关联方采购/销售明细/内部现金流双边核对（ICF-002 流入 vs 对方流出、双向去重）逻辑是否完整。
- 所有**口径参数**（现金等价物科目集合、余额表→报表映射、容差）是否走 `entity_config` 可配置，还是写死在代码里——写死即通用性红线。

### A5. 财务指标阈值审查

- LR-* 规则的阈值（毛利率 [0,1]、同比波动 30%、资产负债率 85%、流动比率 1、收现比 0.8、应收/营收 30%）逐条评估：依据是否写明、是否行业通用、是否需要行业参数化。金融/地产/亏损企业是否会被系统性误报。

### A6. 通用性与不可见硬编码（本项目重点）

- 全库搜索：测试夹具/演示数据是否反向渗入生产代码；真实企业验证中临时加的适配（特定科目、特定格式）是否被泛化还是残留为特例。
- 检查默认值中的环境特定项：LLM 默认端点、更新服务器地址、示例配置是否泄漏公司私有信息。
- 判断标准（重申）：换一家从未见过的企业报表（不同行业、不同科目命名习惯、合并口径），系统应当"能跑、不漏报、不误报、缺数据优雅跳过"。

---

## 3. 角色 B — 全栈工程师审查

> 立场：要对上线稳定性负责的 Tech Lead。关注：数据流是否接通、异常是否有兜底、慢操作是否卡界面、失败是否可感知。

### B1. 数据流与前后端接线（逐链路验证）

- 沿主链路走一遍并验证每一环真实接通：`gui/` 页面操作 → `app_state.py` 信号 → `services/` 编排（ValidationService / DetailValidationService / PackageValidationService / MultiEntityService）→ `core/` 引擎与导入 → `storage/` 持久化 → 结果回传 UI。重点找：**信号未连接的按钮/开关、状态改了但 UI 没刷新、后台结果回来但页面已切换导致丢失**。
- 逐页核查五页（导入/规则/审计底稿/历史记录/设置）的每个可交互控件是否都有实际行为：按钮、开关、筛选标签、搜索框、拖拽区、AI 诊断按钮、导出按钮。列出所有"看起来能点但点了没反应/反应不对"的控件。
- 校验 `AppState` 作为单一状态源的一致性：多页面共享数据（当前报表、校验结果、明细数据集、主题、容差）是否有多个副本导致不一致。

### B2. 异常处理与兜底

- 全库排查：空 `except`、宽 `except Exception`、吞异常后无日志无提示的路径。所有失败最终是否以**中文业务化提示**呈现给用户，还是静默失败/抛出技术堆栈。
- 未捕获异常的全局兜底：GUI 主线程与子线程是否有最后一层防护，崩溃前是否保留现场（已导入数据、已得结果）。
- 文件层失败场景逐个推演：文件被占用、路径含中文/空格、超大文件、非报表 Excel、损坏 PDF、DLP 密文（验证 openpyxl/xlrd 失败 → COM 回退 → 仍失败时统一为中文 FSAError 的链路完整）。
- 多文件导入：单个文件失败是否只记录该文件、不中断整批（验证而非假设）。

### B3. 信息传递效率与一致性

- 校验结果从引擎到 UI 的传递链中，`passed / failed / errored / skipped` 四种语义是否全程不被混淆（统计口径、筛选标签、导出底稿三处数字必须一致）。
- `TraceItem` 的行列定位：从导入（行号记录）→ 求值（trace 组装）→ UI 展示 → 导出底稿，行号是否始终是**原始报表行号**而非中间态索引。
- 设置变更（容差、主题、LLM 配置）的传播路径：QSettings / SQLite override / 内存状态三者是否有读写竞争或生效时机混乱（如改容差后需重启才生效，或"恢复默认"未真正回写）。

### B4. 性能与响应速度

- 大数据量路径实测或代码级评估：序时账数千行导入、六表合并、74+ 项规则一次执行的耗时分布；是否有 O(n²) 或逐行 UI 更新。
- UI 卡顿点：批量重建卡片时是否暂停重绘、输入框防抖是否生效、筛选切换是否闪动、LLM 长响应是否确认在后台线程（主线程零阻塞）。
- SQLite WAL 的读写是否都在合理事务内，是否存在每次操作开关连接。

### B5. Agent / LLM 链路稳定性

- Provider 自动推断（URL → ollama/openai 兼容）的边界：空地址、错误地址、超时、服务不可用的提示是否明确且不阻塞核心校验。
- 无 LLM 时的规则化兜底诊断是否覆盖全部规则族（主表 + 明细各系列），兜底文案是否有财务价值而非套话。
- 工具调用（AgentLoop 9 工具）的失败处理：工具异常、LLM 返回非法 JSON、多轮对话上下文超限。

### B6. 持久化与打包

- SQLite schema 迁移/升级路径（老版本数据库遇到新代码）；历史/会话/覆写三仓库的写入失败处理。
- 打包：PyInstaller 排除项是否完整（ML 重包）、规则库 JSON 与资源文件是否随包分发、安装器静默安装/卸载的完整性、自动更新的 SHA256 校验与失败回滚。

---

## 4. 角色 C — 设计师审查

> 立场：设计评审。基准文档是 `DESIGN_SYSTEM.md`——任何实现与令牌的偏离都算发现。
> 设计哲学基准：克制的专业感、数据为王、呼吸感、层次分明、深色优先。

### C1. 布局与信息架构

- 五页 + 侧边栏 + 顶栏 + AI 抽屉（FAB 触发）的信息架构：核心任务路径（导入 → 校验 → 看差异 → 追溯 → 导出）是否三步内可达；次要功能（历史、设置）是否不干扰主路径。
- 逐页检查：栅格对齐、留白节奏、卡片间距一致性；空状态、加载态、数据过多态（长列表/宽表格）是否都有设计。
- 窗口缩放行为：最小宽度、抽屉拖拽区间、表格横向滚动、侧边栏窄屏表现。

### C2. 色彩系统合规

- 对照 `DESIGN_SYSTEM.md` 令牌逐项核对实现：品牌色（靛蓝）、语义色（success/error/warning/info）、金额色（负数红）、中性色层级。找出**硬编码色值**（绕过令牌的 hex）与令牌误用（如用 error 红表示普通强调）。
- 明暗双主题：每个页面逐一切换验证，重点查深色下的对比度（正文/次要文本/边框）、语义色在深色底的辨识度、半透明顶栏的叠加效果。
- 状态色语义一致性：通过=绿、错误=红、警告=黄在全应用（卡片、角标、表格、筛选标签、导出文件）是否统一；"异常(errored)"的呈现是否与"不通过"视觉上可区分（呼应 B3/A2）。

### C3. 字体与排版

- 字体栈（HarmonyOS Sans SC 优先）的回退链；高 DPI 下是否发虚（已有 RoundPreferFloor + hinting 修复，验证实际效果）。
- 字号层级是否成体系：页面标题 / 副标题 / 汇总数字 / 正文 / 辅助文本 / 表格，各层级字号、字重、行高是否一致应用；金额数字是否等宽对齐（表格中右对齐、千分位）。
- 长文本处理：规则名、错误信息、AI 回答的截断/换行/悬浮全文策略。

### C4. 组件规范

- 校验结果卡片（左边框语义色 + badge + 金额 + AI 按钮）、汇总卡片、规则卡片、报表卡片：四者视觉语言是否统一（圆角、阴影、边框、内边距）。
- 阴影使用是否克制（设计哲学要求层次靠颜色/字号/间距而非重阴影）；悬浮/按压/禁用三态是否所有可点元素都具备。
- 图标：是否全量使用 FluentIcon 且无 Emoji 残留；图标语义是否与财务含义匹配。
- 表格（审计底稿页、历史页）：斑马纹/悬浮行/表头吸顶/列宽策略/空表状态。

### C5. 动效与微交互

- 动效清单与实际表现：抽屉滑出、卡片 hover、按钮按压、页面切换、主题切换过渡——时长与缓动是否统一、有无闪烁/跳动/布局位移。
- 反馈及时性：点击后 100ms 内是否有视觉反馈；耗时操作（导入、校验、导出、LLM）是否有进度/加载指示且可感知剩余状态。
- 破坏性/不可逆操作（重置、清空会话、删除自定义规则）是否有确认与后果说明。

### C6. 文案审查（与 A 角色的财务口径联动）

- 全部用户可见文案：财务术语是否准确（不通过/异常/跳过 vs 失败/错误/警告混用）、有无技术黑话（"求值失败""变量未定义"应转译为财务语言）、有无英文残留。
- AI 助手与诊断建议的文案：是否给出可操作的财务指引，语气是否符合"辅助专业判断"而非"替代判断"。

---

## 5. 角色 D — 交叉审查（用户未点名但重要的维度）

### D1. 数值精度与计算正确性

- 金额用 `float` 的边界：大数（1e12+，茅台级年报）、极小差额、连加累计误差在 `abs(a-b) <= tolerance` 下是否始终安全；相对容差的基准值取哪一侧、基准为 0 时如何退化。
- 导入层数值清洗：千分位、括号负数、"-"占位符、科学计数法、NaN→0/None 的转换是否可能把"空"误转为"0"而制造虚假平衡（P1 反向风险：漏报）。

### D2. 可追溯性（P3）端到端验证

- 抽样若干条结果，从 UI 点击差异 → trace 展示 → 导出底稿 → 回到原始 Excel 行列，人工核对定位是否**逐位准确**（含 BS 双栏、期间列模式、SCE 矩阵、PDF 来源的行号语义）。

### D3. 测试的有效性（而非仅覆盖率）

- 测试是否真正断言**财务结论**（通过/不通过/跳过/异常及其金额），还是只断言"不抛异常"；真实年报回归（茅台/格力）是自动化断言还是人工看一眼。
- 测试数据与生产逻辑的隔离：conftest 工厂函数是否简化了关键路径而导致漏测（如双金额列、SCE、明细六表在测试中的覆盖真实性）。

### D4. 安全与数据边界

- 财务数据的离线承诺是否成立：除显式配置的 LLM 端点与更新地址外，是否有任何外发请求；LLM 请求中发送的报表数据范围是否最小化且有用户预期。
- 打包产物中是否混入开发期产物（演示数据、真实企业报表、API key、调试日志）；`.gitignore` 与打包排除项双重核查。

### D5. 文档与代码的一致性

- `AGENTS.md` / `README.md` / `HANDOFF.md` / `DESIGN_SYSTEM.md` 中的规则条数、依赖清单、功能状态、命令是否与代码现实一致；不一致处即文档债，列清单。

---

## 6. 输出格式（严格遵守）

```markdown
# 审查报告 — <日期>

## 一、结论摘要
- 总体评级：可发布 / 修复 P0/P1 后可发布 / 需重构
- 三个角色各一句话总评（财务合规 / 工程稳定 / 设计完成度）
- 通用性红线命中数：<n> 处

## 二、P0 / P1 必须修复清单（置顶，按级别排序）
| # | 级别 | 维度 | 标题 | 证据（文件:行 / 规则ID） | 影响 | 建议 |
|---|------|------|------|--------------------------|------|------|

## 三、分维度详细发现
### A. 财务专家（合规性）
（每条发现：标题 / 证据 / 财务影响 / 准则依据 / 建议 / 是否设计意图待确认）
### B. 全栈工程
### C. 设计
### D. 交叉

## 四、通用性硬编码专项清单
（所有"换一家企业就出错"的点，集中列出）

## 五、观察到的良好实践（简短，防止误改）
（有意为之的保守设计，标注"勿动"）
```

要求：
- P2/P3 发现可合并同类项，但 P0/P1 必须逐条独立。
- 每条建议给出**最小改动方向**，不写大段重构方案。
- 拿不准的先验证再报告；无法验证的标注"（未验证，需人工确认）"，不得臆断为缺陷。

---

## 7. 使用建议

- 一次性全量审查：将本文件 + 代码库交给具备长上下文的审查者，按 A → B → C → D 顺序执行（财务优先）。
- 增量审查：只取某一角色的章节 + 第 0/1/6 节（纪律、背景、输出格式）即可独立使用。
- 已知历史验证基线（供比对，不作为免检项）：茅台/格力年报 0 失败 0 异常；真实六表包曾定位出若干**真实**经营差异（如投资支付现金明细与主表差 1500 万、重分类坏账口径差异）——这些是系统能力的证据，审查时勿将其误判为系统缺陷。
