# 财务报表勾稽校验系统 — 端到端全面审查 Prompt (E2E v2)

> **用途**：将本文件作为任务指令交给审查编排者（AI Agent 或人），对本项目执行一次端到端深度审查。
> **审查对象**：当前工作树全量（含 `fix/review-2026-08-13` 分支未提交修改）——`src/fsa/`、`tests/`、`cas_gouji_rule_library.json`、`scripts/`、打包配置、GUI 视觉表现。
> **本 Prompt 自包含**：审查者无需外部上下文即可开工。
> **与前版关系**：`REVIEW_PROMPT.md`（2026-08-15 归档版）的角色 A/B/C/D 框架仍然有效，本版在其上新增：**截图证据驱动的视觉审查**、**功能逐项评分与可用性判定**，并将全部事实数据校准到 2026-08-16 现状。

---

## 0. 审查纪律（先于一切角色）

1. **只报告，不改代码。** 唯一例外：为生成截图允许向 `tests/fixtures/real_reports/`（已 gitignore）放置/复制 fixture 文件，以及对 `scripts/ux_shots.py` 的硬编码路径做一次性本地替换——这些不计入代码改动，审查结束后须还原。
2. **每条发现必须有证据**：文件路径 + 行号、规则 ID + 公式原文、或截图文件名 + 图中位置。没有证据的"感觉有问题"不写入报告。
3. **严重级别**：
   - **P0 阻断**：财务结论可能错误、数据可能丢失/污染、应用崩溃、合规硬伤、主流程断链。
   - **P1 高**：特定场景错误结果或误导性提示、核心功能不可用、构建/测试门禁失效。
   - **P2 中**：边界场景缺陷、体验明显受损、性能问题、视觉明显失真。
   - **P3 低**：规范偏离、可维护性、轻微体验/视觉瑕疵。
4. **区分"缺陷"与"设计权衡"**：项目有明确的保守设计原则（§1 的 P1-P6）。疑似权衡的标注"（设计意图待确认）"，不得直接判为缺陷。
5. **通用性红线**：任何"只在当前测试数据/当前企业/当前机器上成立"的逻辑都是审查目标（写死科目编码、固定行列号、特定表头文本、环境私有地址、无出处魔法数字）。判断标准：换一家不同行业、不同格式的企业报表，该逻辑是否仍然成立。
6. **评分纪律（本版新增）**：功能评分必须逐项给出证据（测试、脚本实测、代码路径），禁止印象分。无法验证的维度标"未验证"并按 0 分计入，同时在报告中注明。
7. **输出全中文**，面向项目负责人（财务+技术双语读者）。

---

## 1. 背景速览（2026-08-16 校准后事实，以此为准）

- **定位**：离线 Windows 桌面软件。导入企业财务报表（Excel/PDF）→ 按 CAS 规则库勾稽校验 → 差异追溯 → Excel 审计底稿导出 → 可选 LLM 诊断。
- **技术栈**：PySide6 + qfluentwidgets（GUI）、pandas + openpyxl + xlrd（Excel）、pdfplumber（PDF）、simpleeval（规则求值）、SQLite WAL（持久化）、stdlib urllib（更新/LLM，无 requests）。
- **现行关键数字**：规则库 **42 条 v1.3.0**（A-表内平衡 / B-表间勾稽 / C-逻辑合理性）；测试 **1427 收集 + 22 slow**；覆盖率门禁 core/services/storage ≥90%、整体 ≥84%（CI）；当前版本 **0.4.1**。
- **模块图**（`src/fsa/`）：`core/engine`（42 条 JSON 规则 RuleRunner + 12 组明细检查 *_checks.py）、`core/importer`（Excel/PDF/SCE/明细六表导入，COM 回退抗 DLP 加密）、`core/exporter`（审计底稿**四表**：汇总/明细/科目追溯/底稿说明）、`core/models`（数据契约层，空 `__init__.py` 深导入约定）、`core/` 顶层（exceptions/version/resources/edition 双版本通道/logging）、`services`（Validation/PackageValidation/DetailValidation/MultiEntity/EntityConfig/ProblemPackage）、`agent`（AgentLoop/Debate/诊断/知识库/消毒）、`storage`（history/chat/override 三仓库）、`updater`（version.json + SHA256 + 静默安装）、`gui`（五页 + 侧栏 + 顶栏 + AI 抽屉）。
- **设计原则（设计意图基准）**：P1 宁可漏报不可误报（缺数据→跳过）；P2 确定性；P3 可溯源（TraceItem 行列）；P4/P6 面向财务用户全中文；P5 模块隔离（引擎只认 Report）。
- **精度机制**：金额接口 float；**容差比较内部在 Decimal 域执行**（`str()` 转换防二进制尾数污染）；相对容差基准为 0 且左值非 0 → RelativeBaseZeroError → 走 skip 路径。
- **已知验证基线**：真实年报（茅台/格力 2023）42 条规则 0 失败 0 异常（需 fixture 才可复验）；真实六表包曾定位出**真实**经营差异（系统能力证据，勿误判为缺陷）。

### 1.1 已知问题种子清单（2026-08-16 结构审查发现——各 lane 验证定性，不要重新发现）

| # | 线索 | 指派 lane |
|---|---|---|
| S1 | 工作分支存在 mypy/ruff 级类型错误：`excel_reader.py:305`（ModuleType 未定义）、`main_window_debate.py:122/124`（mixin 属性）、`agent_drawer.py:457`（QResizeEvent 未定义）、`audit_page.py:226`、`excel_reader.py:134/170`——跑 mypy/ruff 确认真伪与运行时影响 | B |
| S2 | `import_page.py` 5 处 `except Exception` 宽 catch（:380/438/444/616/714）——逐个判断可否收窄 | B |
| S3 | `excel_reader.py:419` `# type: ignore[return-value]`——根 §3.1 规定 type: ignore 仅限 gui/ 的现存唯一例外 | B/D |
| S4 | **打包版无外部知识文档**：fsa.spec datas 未含 `resources/knowledge/`，冻结模式 `_external_knowledge()` 静默返回空——Agent 知识检索在打包版降级 | B/D |
| S5 | `compute_sha256` 双实现（updater 抛 OSError vs resources.sha256_file 返回 ""） | D |
| S6 | `tests/gui/` 无 `__init__.py`（其他测试目录都有） | D |
| S7 | 根 `custom_rules.json`（17 字节占位）在仓库根——确认是运行时产物误入 git 还是有意为之 | D |

---

## 2. 执行架构（多智能体编排）

### Phase 0 — 证据采集（编排者先行，全部留档到报告附录）

```bash
python -m pytest -q                      # 全量测试结果（期望 1427 passed）
python -m ruff check src/ tests/         # lint 现状
python -m mypy src/fsa                   # 类型检查现状（对照种子 S1）
python -m pytest --cov=src/fsa --cov-report=term-missing   # 覆盖率现状
python scripts/ux_shots.py               # 生成视觉审查截图（见 §3 Lane C 前置条件）
```

### Phase 1 — 五条 lane 并行

| Lane | 角色 | 建议执行体 | 输入 |
|---|---|---|---|
| A | 财务合规审查 | 高推理 agent（oracle/deep） | 规则库 JSON + 引擎/导入代码 |
| B | 工程与接线审查 | 自主执行 agent（deep/unspecified-high） | 全代码 + Phase 0 结果 |
| C | 视觉审查（识图） | **识图智能体**（multimodal-looker / look_at / 图像分析工具） | ux_shots/ 截图 + DESIGN_SYSTEM.md |
| D | 交叉审查 | 高推理 agent | 全代码 + 种子清单 |
| E | 可用性评分 | 编排者汇总时执行 | A-D 的全部发现 + Phase 0 证据 |

### Phase 2 — 汇总评级

编排者合并五条 lane，按 §5 输出格式出报告，Lane E 评分必须有 A-D 证据支撑。

---

## 3. Lane A — 财务合规审查（最高优先级）

> 立场：签字会计师。你要为"这份校验结果能不能拿去用"负责。财务发现置前。

- **A1. 42 条规则逐条核对**：公式语义 vs CAS 准则原文（重点：利润表 2019 多步式各减值科目的**符号方向**、CF 补充资料 `cf_notes_` 勾稽、SCE 矩阵 `sce_` 映射位置）；`cas_ref` 引用真实对题不虚构；容差类型与业务语义匹配（exact/relative/threshold 用对没有，BS-BAL-004 相对容差 0.1% 的理由）。
- **A2. P1 落地核查**：找出所有"缺数据报错而非跳过"路径（变量未定义、除零、期初缺失、单体报表遇合并专属变量）；`errored` 与"不通过"在结果/统计/UI/导出四处严格区分；LR-* 规则前置守卫是否足够保守（负毛利企业、金融/地产行业负债率会不会系统性误报）；`KNOWN_LINE_ITEM_KEYS` 零值预填的排除清单（`models/result.py` 注释）逐项复核。
- **A3. 科目映射财务正确性**：`name_mapper.py` 易混科目抽查（其他综合收益 vs 综合收益总额、信用减值符号、库存股减项、少数股东权益）；`clean_name` 清洗管线有无误伤合并两个科目；未映射科目静默丢弃的可审计性（`unmapped_names` 是否足够可见）。
- **A4. 明细勾稽（L2/L4/附表3-6）**：凭证借贷平衡、现金流明细↔主表/序时账、余额表↔资产负债表的口径成立性；重分类"调表不调账"实务符合度；坏账准备口径差异是否提示而非报错；ICF-002 双边核对与双向去重完整性；口径参数是否全走 `EntityConfig` 可配置（写死=通用性红线）。
- **A5. 行业阈值**：`engine/thresholds.py` 参数化机制审查——行业取值依据、未知行业回落行为、`EntityConfig.industry` 覆写链是否完整到 runner 注入。

## 3. Lane B — 工程与前后端接线审查

> 立场：对上线稳定性负责的 Tech Lead。本 lane 回应用户关注点 2 的"接线"部分。

- **B1. 主链路逐环验证**（要证据，不要推断）：
  `import_page` → `ImportService` → `PackageValidationService.validate`（GUI 唯一校验入口）→ `ValidationService` + `DetailValidationService` → `merge_summaries` → AppState 信号 → 各页刷新 → `export_helper` → `AuditExporter` → `history_repo` 持久化。
- **B2. 控件行为清单（强制产出表格）**：逐页（导入/校验结果(审计)/规则/历史/设置）+ 侧栏/顶栏/AI 抽屉/对话框，列出**每个可交互控件**（按钮、开关、筛选标签、搜索框、拖拽区、AI 诊断、导出、期间选择器、主题切换、问题包导出）→ 实际行为 → 验证方式。找出所有"看起来能点但点了没反应/反应不对"的控件。
- **B3. 状态一致性**：`AppState` 单一状态源核查——报表/结果/明细/主题/容差有无第二副本；QSettings ↔ SQLite override ↔ 内存三者的读写竞争与生效时机（改容差后是否立即生效、"恢复默认"是否真回写）；存储降级为 None 时各页判空表现。
- **B4. 异常与兜底**：空 catch / 宽 catch 全库清单（含种子 S2 五处逐个定性）；未捕获异常的最后防线（主线程 + worker 线程）；文件失败场景推演（占用/中文路径/超大/非报表 Excel/损坏 PDF/DLP 密文 → COM 回退 → 统一中文 FSAError）；多文件导入单文件失败是否不中断整批。
- **B5. 线程与性能**：`threading.Thread` + `invokeMethod(QueuedConnection)` 模式是否全程遵守（无 QThread 混入）；LLM 任务全部后台化、旧任务取消、closeEvent 先取消 worker；批量重建 `setUpdatesEnabled(False)`、筛选不重建卡片等约定执行度；参照 `benchmark_app.py`/`benchmark_import.py` 实测或代码级评估大数据路径。
- **B6. 持久化/更新/打包/双版本**：SQLite DDL 幂等与老库升级；updater 的 SHA256 尽力而为 + install() 三秒延迟契约（GUI 须立即退出）是否被遵守；edition 域检查（USERDNSDOMAIN/USERDOMAIN 回退逻辑）的绕过风险；fsa.spec 排除项与 datas 完整性（含种子 S4 知识库缺口定性：是缺陷还是发布策略）；CI 门禁（ruff+mypy+pytest+coverage 双门槛+打包 artifact）本地复跑验证。

## 3. Lane C — 视觉审查（识图智能体，用户关注点 2 的前端细节）

> 立场：设计评审。基准文档 `DESIGN_SYSTEM.md`；设计哲学：克制的专业感、数据为王、呼吸感、层次分明、深色优先。
> **本 lane 必须以截图为证据**，纯代码推断的视觉结论不写入报告。

**前置（截图生成）**：
1. `tests/fixtures/real_reports/贵州茅台_2023年报_三大报表.xlsx` 当前**不存在**（合规红线已移出 git）。将 `tests/fixtures/realistic_report.xlsx` 复制到该路径（目录已 gitignore，安全），或把 `scripts/ux_shots.py:20` 的路径一次性替换为 realistic_report.xlsx。
2. 项目根目录运行 `python scripts/ux_shots.py`，产出 13 张截图到 `ux_shots/`：5 页 × 明暗双主题 + AI 抽屉 ×2 + 展开卡片 ×1。
3. 若需补充状态（空状态/错误状态/不通过卡片），可用 `scripts/verify_w3.py` 或手写等价 offscreen 脚本补拍。

**审查项（逐截图核对）**：
- **C1 布局与信息架构**：核心路径（导入→校验→看差异→追溯→导出）三步可达性；栅格对齐、留白节奏、卡片间距一致性；空/加载/数据过多态；窗口缩放行为（最小宽度、抽屉拖拽、表格横滚）。
- **C2 配色合规**：对照 DESIGN_SYSTEM.md 令牌逐项核对——品牌靛蓝 `BRAND_500=#5b5ee6`、语义色（通过绿/不通过红/警告黄/异常如何区分）、金额负数红、中性色层级；找硬编码 hex 与令牌误用；深色下对比度（正文/次要/边框）与语义色辨识度。
- **C3 图标**：全量 FluentIcon 无 Emoji 残留；图标语义与财务含义匹配；侧栏选中态、按钮图标+文字组合、FAB 样式。
- **C4 排版**：字号层级体系（标题/副标题/汇总数字/正文/辅助/表格）；金额等宽右对齐千分位；高 DPI 发虚检查；**CJK 文本裁切/溢出逐张排查**（长规则名、错误信息、按钮文字）。
- **C5 组件一致性**：结果卡片/汇总卡片/规则卡片/报表卡片四者视觉语言统一（圆角、边框、内边距、阴影克制）；悬浮/按压/禁用三态；表格斑马纹/表头/空表状态。
- **C6 文案（与 Lane A 联动）**：截图中全部可见文案——术语准确（不通过/异常/跳过不混用）、无技术黑话、无英文残留、无错别字。
- **执行方式**：每张截图至少过两轮——**功能完整轮**（该有的元素在不在、能不能看懂状态）+ **保真轮**（对齐/间距/裁切/对比度像素级）。明暗主题对照着看，深色特有的对比度问题单独列。

## 4. Lane D — 交叉审查

- **D1. 数值精度**：Decimal 迁移后的边界复核——`str(float)` 转换对科学计数法/inf 的处理；1e14 预警阈值语义（只告警不改判定）；导入层"空 vs 0"（amount_parser 区分真空值 None 与占位符 0.0）有无反向漏报风险。
- **D2. 可追溯端到端抽样**：抽 3-5 条结果，UI 差异 → trace → 导出底稿 → 原始 Excel 行列，逐位人工核对（含 BS 双栏、期间列、SCE 矩阵、PDF 行号 `页码*10_000_000+行` 编码）。
- **D3. 测试有效性**：断言的是财务结论还是"不抛异常"；真实年报回归是自动断言还是人工看；conftest 工厂是否简化关键路径导致漏测；种子 S6。
- **D4. 安全与数据边界**：离线承诺（除 LLM 端点与更新地址外无外发请求）；LLM 请求发送的报表数据最小化；`sanitize.py` 防注入围栏有效性；打包产物有无混入开发产物/API key；种子 S5/S7。
- **D5. 文档一致性**：README/HANDOFF/DEV_LOG/DESIGN_SYSTEM/TEST_PLAN 与代码现实对照（根 AGENTS.md 与各模块 AGENTS.md 已于 2026-08-16 校准，可跳过，重点查其余文档残留）；`.architecture-audit/` 旧产物是否误导。

## 5. Lane E — 功能评分与可用性判定（本版核心新增）

> 立场：产品负责人验收。回答用户的问题：**各项功能几分？整体能不能用？**

### 评分维度（每项功能百分制，四维度加权）

| 维度 | 权重 | 评分依据 |
|---|---|---|
| 完整性 | 30 | 承诺的功能点是否都实现并接通（对照 README/AGENTS.md §9） |
| 正确性证据 | 30 | 有无测试覆盖 + 真实数据验证 + 本次审查实测；缺证据按 0 计 |
| 健壮性 | 20 | 异常路径兜底、边界场景、降级行为 |
| 易用性 | 20 | 财务用户能否不看代码独立完成操作（含文案、错误提示、视觉可懂度——引用 Lane C 证据） |

### 功能清单（13 项，逐项打分）

1. Excel 导入（.xlsx/.xls/双栏/期间列/DLP 回退） 2. PDF 导入 3. SCE 权益变动表 4. 明细六表勾稽（L2/L4/附表3-6） 5. 规则引擎（42 条 + 行业阈值参数化 + 自定义规则） 6. 结果展示与追溯（卡片/trace/筛选） 7. 审计底稿导出（四表 + SHA256 留痕） 8. Agent 诊断（LLM + 辩论 + 兜底 + 知识库） 9. 历史与持久化 10. 设置与容差覆写 11. 自动更新 12. 多主体批量校验 13. 打包与发布（安装器/签名/冒烟/双版本通道）

### 可用性判定标准

- **每功能**：≥85 生产可用 / 70-84 内部试点可用 / 60-69 演示级 / <60 不可用
- **整体评级**（取功能加权 + 一票否决）：
  - **生产可用候选**：无 P0、P1 全部有变通方案、核心功能（1/5/6/7）均 ≥85
  - **内部试点可用**：无 P0、核心功能 ≥70
  - **演示级**：有 P1 未解或核心功能 <70
  - **一票否决**：任何 P0 未修复 → 不可发布，无论均分

## 6. 输出格式（严格遵守）

```markdown
# 端到端审查报告 — <日期>

## 一、结论摘要
- 总体评级：生产可用候选 / 内部试点可用 / 演示级 / 不可发布
- 四 lane 各一句话总评（财务合规 / 工程接线 / 视觉 / 交叉）
- 通用性红线命中数：<n>；P0 <n> 个 / P1 <n> 个 / P2 <n> 个 / P3 <n> 个

## 二、功能评分矩阵（置顶表格）
| 功能 | 完整性/30 | 正确性/30 | 健壮性/20 | 易用性/20 | 总分 | 等级 | 关键证据 |
|---|---|---|---|---|---|---|---|

## 三、P0 / P1 必须修复清单（按级别排序）
| # | 级别 | 维度 | 标题 | 证据（文件:行/规则ID/截图） | 影响 | 最小修复建议 |
|---|---|---|---|---|---|---|

## 四、分维度详细发现
### A. 财务合规  ### B. 工程接线（含控件行为清单表）  ### C. 视觉（每条注明截图文件名）  ### D. 交叉

## 五、通用性硬编码专项清单

## 六、已知问题种子核查结论（S1-S7 逐条：确认/排除/部分确认 + 定性）

## 七、观察到的良好实践（标注"勿动"）

## 八、附录：Phase 0 命令输出摘要（pytest/ruff/mypy/覆盖率数字）
```

要求：P2/P3 可合并同类项，P0/P1 必须逐条独立；每条建议给最小改动方向；拿不准先验证再报告，无法验证标"（未验证，需人工确认）"，不得臆断为缺陷。

---

## 7. 使用建议

- **全量审查**：编排者跑 Phase 0 → 并行发 A/B/C/D 四 lane（C 派识图智能体）→ 收齐后执行 Lane E 汇总出报告。
- **增量审查**：单 lane + 第 0/1/6 节即可独立使用；修复后复审只跑对应 lane + Lane E 重评分。
- **OpenCode 执行参考**：Lane A/D 用 `oracle` 或 `deep`；Lane B 用 `deep`（需跑命令）；Lane C 用 `multimodal-looker` 逐张读图（或主会话 `look_at`）；均 `run_in_background=true` 并行，收齐再汇总。
- **复审基线**：本文件种子清单 S1-S7 来自 2026-08-16 结构审查；下轮审查前先更新种子清单。
